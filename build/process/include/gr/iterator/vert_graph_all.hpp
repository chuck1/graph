/*
 * DO NOT EDIT THIS FILE
 *
 * graph/include/gr/iterator/vert_graph_all.hpp_in
 */

#ifndef GR_ITERATOR_VERT_GRAPH_ALL_HPP_IN
#define GR_ITERATOR_VERT_GRAPH_ALL_HPP_IN

#include <iterator>

#include <gr/decl.hpp> // gr/decl.hpp.in
#include <gr/vert.hpp> // gr/vert.hpp.in
#include <gr/vert_comp.hpp> // gr/vert_comp.hpp.in
//#include <gr/edge.hpp> // gr/edge.hpp.in

namespace gr {
namespace iterator {

typedef std::iterator<std::input_iterator_tag, gr::VERT_S> ITERATOR_VERT;

/**
 * iterates on each edge in a vert's edge container
 */
class vert_graph_all:
	public ITERATOR_VERT
{
public:
	typedef typename gr::CONT_VERT::iterator iterator;

	typedef gr::VERT_S const &	reference;
	typedef gr::VERT_S		value_type;

	vert_graph_all(gr::container::vert &, iterator);
	vert_graph_all(gr::container::vert &, iterator, gr::VERT_FUNC);

	vert_graph_all			operator=(vert_graph_all const &);

	void				next();

	gr::iterator::vert_graph_all	operator++();
	/** postfix */
	gr::iterator::vert_graph_all	operator++(int);
	
	reference			operator*();
	value_type const *		operator->();

	bool				operator==(gr::iterator::vert_graph_all const &);
	bool				operator!=(gr::iterator::vert_graph_all const &);

//private:
	gr::container::vert &		_M_container;
	iterator			_M_j;
	gr::VERT_FUNC			_M_func;
};


}}

#endif
